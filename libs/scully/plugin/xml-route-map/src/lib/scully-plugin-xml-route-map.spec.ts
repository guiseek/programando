import { scullyPluginXmlRouteMap } from './scully-plugin-xml-route-map';

describe('scullyPluginXmlRouteMap', () => {
  it('should work', () => {
    expect(scullyPluginXmlRouteMap()).toEqual('scully-plugin-xml-route-map');
  });
});
