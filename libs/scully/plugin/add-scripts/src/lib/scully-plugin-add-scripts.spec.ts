import { scullyPluginAddScripts } from './scully-plugin-add-scripts';

describe('scullyPluginAddScripts', () => {
  it('should work', () => {
    expect(scullyPluginAddScripts()).toEqual('scully-plugin-add-scripts');
  });
});
