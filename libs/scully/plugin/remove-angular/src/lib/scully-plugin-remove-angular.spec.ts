import { scullyPluginRemoveAngular } from './scully-plugin-remove-angular';

describe('scullyPluginRemoveAngular', () => {
  it('should work', () => {
    expect(scullyPluginRemoveAngular()).toEqual('scully-plugin-remove-angular');
  });
});
